var inputs = [...];

function setupVolumes(inputs, callback) {
	var volume = null
	inputs.forEach(function (input) {
		var parsed = parseVolumeCommand(input);
		if (parsed) {
			volume = parsed;
		}
	});

	// DO WORK AND CALL NEXT
}
